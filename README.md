# self-pruning-neural-network
Self-pruning neural network for CIFAR-10 classification using learnable gate parameters and L1 sparsity regularization, enabling dynamic model compression during training (PyTorch implementation).
